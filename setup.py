from setuptools import setup

setup(name='implementations',
      install_requires=[
          'gym',
          'scipy',
          'tensorflow>=1.4.0',
          'opencv-python'
      ],
      version='0.0.1')
